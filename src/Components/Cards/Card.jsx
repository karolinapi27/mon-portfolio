import React from 'react';

const Card = ({ title, cover, description, host, rating, location, tags }) => {
    return (
        <div className="card">
            {}
        </div>
    );
};

export default Card;
